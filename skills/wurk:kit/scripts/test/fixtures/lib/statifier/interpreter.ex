# A fixture file whose only job is to exist at this path, so
# permalinks_test.rb's CLI tests can prove a referenced path resolves
# under the fixture's computed repo root (test/fixtures - see
# ManifestHelper). Content is irrelevant; only File.exist? is checked.
